/**
 * OpenAI client initialization.
 * Configured from environment variables (server-side only).
 */

// Placeholder — will be implemented in the AI categorization feature
export function getOpenAIClient() {
  // TODO: Initialize OpenAI client with environment variables
  throw new Error("OpenAI client not yet configured");
}
